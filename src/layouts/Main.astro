---
import Layout from '../layouts/Layout.astro';

import { icon } from '@fortawesome/fontawesome-svg-core';

import { faBriefcase, faEnvelope} from '@fortawesome/free-solid-svg-icons';
import { faTelegram } from '@fortawesome/free-brands-svg-icons';

const PortfolioIcon = icon(faBriefcase).html;
const TelegramIcon = icon(faTelegram).html;
const EmailIcon = icon(faEnvelope).html;

interface Props {
    name: string;
    fullname: string;
    about: string;
    /*portfolio: string;*/
    telegram: string;
    email: string;
}

const { name, fullname, about, /*portfolio,*/ telegram, email } = Astro.props;

---

<Layout title="arlcrow123 — Matvey Aleksandrov">
    <div class="text-center z-10" id="container">
        <img class="h-[25vh] object-contain opacity-80 mx-auto" src="/images/logo.svg" alt="Logo" id="logo">
        <div class="mt-10 text-4xl font-bold">{name}</div>
        <div class="text-2xl font-medium">{fullname}</div>
        <hr class="mt-2 w-full border-0 border-b-2 border-dashed border-white">
        <div class="mt-2">{about}</div>
        <div class="mt-14 flex justify-center">
            <!-- TODO: portfolio (about) website
            <div>
                <a href="https://about.arlcrow.site" class="flex group">
                    <div class="mr-1 text-gray-500 group-hover:text-white">[</div>
                    <div class="w-5 mr-1 group-hover:text-gray-500" set:html={PortfolioIcon}></div>
                    <div class="mr-1 group-hover:text-gray-500">{portfolio}</div>
                    <div class="text-gray-500 group-hover:text-white">]</div>
                </a>
            </div>-->
            <div>
                <a href="https://t.me/arlcrow123" class="flex group">
                    <div class="mr-1 text-gray-500 group-hover:text-white">[</div>
                    <div class="w-5 mr-1 group-hover:text-gray-500" set:html={TelegramIcon}></div>
                    <div class="mr-1 group-hover:text-gray-500">{telegram}</div>
                    <div class="text-gray-500 group-hover:text-white">]</div>
                </a>
            </div>
            <div>
                <a href="mailto:matvey@arlcrow.site" class="flex group">
                    <div class="mr-1 text-gray-500 group-hover:text-white">[</div>
                    <div class="w-5 mr-1 group-hover:text-gray-500" set:html={EmailIcon}></div>
                    <div class="mr-1 group-hover:text-gray-500">{email}</div>
                    <div class="text-gray-500 group-hover:text-white">]</div>
                </a>
            </div>
        </div>
    </div>
</Layout>
